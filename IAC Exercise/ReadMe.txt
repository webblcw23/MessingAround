An IaC exercise using Bicep as well as ARM template (.json) to create resources.
Also, a bicep IaC file to create resources (web app) with the use of declaring and calling the delcared variabels to deploy env specific resources to the correct env. With the optional useage of a YAML pipieline file.
This can be added to Azure DevOps to run this file automatically.